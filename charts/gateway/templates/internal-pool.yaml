apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: internal-pool
spec:
  cidrs:
    - cidr: "10.220.147.100/24"  # 전체 서브넷 지정
  serviceSelector:
    matchLabels:
      io.cilium/lb-ipam-pool: internal-pool