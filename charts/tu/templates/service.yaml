apiVersion: v1
kind: Service
metadata:
  name: {{ include "tu.fullname" . }}
  labels:
    {{- include "tu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tu.selectorLabels" . | nindent 4 }}